#include <bits/stdc++.h>
using namespace std;

#define akitama return 0
#define ll long long

void ans(bool answer) {
    answer? cout << "YES\n" : cout << "NO\n";
}

void solve() {
    bool answer = false;
    
    ans(answer);
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;cin >> t;
    while(t --) {
        solve();
    }

    akitama;   
}