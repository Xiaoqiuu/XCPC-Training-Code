#include <iostream>
#include <algorithm>
#include <cmath>
#include <cstring>
#include <vector>
#include <cstdlib>
using namespace std;
using ll = long long int;
#define akitama return 0
#define vi vector<int>
void solve() {
    int n, k; cin >> n >> k;
    string s; cin >> s;
    vi cnt(k+1, 0);
    for (int i = 0; i < n; ++ i) {
        // cnt[s[i] - 'a' + 1] ++;
    }
    int q; cin >> q;
    while (q--)
}