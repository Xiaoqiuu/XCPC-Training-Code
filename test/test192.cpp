#include <bitset>
#include <iostream>
#include <bitset>

using namespace std;

#define akitama return 0

#define ll long long int
#define ld long double

void solve();

signed main(){
    cin.tie(nullptr)->
    ios::sync_with_stdio(false);
    int _ = 1; // cin >> _;
    while (_--) {
        solve();
    }
    akitama;
}

void solve() {
    bitset<32> a;
}