#include <stdio.h>

int
sum(const int a[]) {
    return a[0] + a[1];
}

signed
main() {
    // int a[2];
    // scanf("%d %d", &a[0], &a[1]);
    // printf("%d\n",  sum(a));

    printf((NULL == 2.0/3?"1":"0"));
}