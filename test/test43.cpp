#include <iostream>
using namespace std;

#define loli long long
#define akitama return 0

#define square(x) ((x) * (x))

void solve() {
    int a; cin >> a;
    cout << square(a) << endl;
    
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int _ = 1;
    while (_) {
        solve();
        _--;
    }

    akitama;
}