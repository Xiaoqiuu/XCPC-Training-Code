#include <bits/stdc++.h>
using namespace std;

signed main() {
    set<int> s;
    s.insert(1);
    cout << "egws: " << s.size() << '\n';
    s.insert(1);
    // s.insert(2);
    cout << "egws: " << s.size() << '\n';
    
}