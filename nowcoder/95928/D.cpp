#include <iostream>
#include <algorithm>
#include <cmath>
using namespace std;
void solve() {
    int n;cin >> n;
    int a[n];
    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
    sort(a, a + n);
    //1 9 55 494

}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    return 0;
}