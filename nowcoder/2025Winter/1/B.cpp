#include <iostream>
using namespace std;

#define akitama return 0
#define ll long long

struct ListNode {
    int val;
    ListNode *next;
    ListNode(int x) : val(x), next(nullptr) {}
};

void solve() {
    
}
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;cin >> t;
    while (t --) {
        solve();
    }
    akitama;
}