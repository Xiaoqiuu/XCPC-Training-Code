#include <iostream>
#include <cstring>
#include <cmath>
using namespace std;
using ll = long long;

void solve(){
    ll a, b, c;
        scanf("%lld,%lld,...,%lld", &a, &b, &c);
            cout << c - a << endl;
            }

            int main() {
                ios::sync_with_stdio(false);
                    cin.tie(nullptr);
                        solve();
                            return 0;
                            }