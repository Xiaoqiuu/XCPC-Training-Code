#include <iostream>
#include <cmath>
using namespace std;
using ll = long long;

void solve(){
    ll n, h;
    cin >> n >> h;
    ll a[n], b[n], c[n];
    for (int i = 0; i<n; ++ i){
        scanf("%lld %lld %lld", &a[i], &b[i], &c[i]);
    }
    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
    return 0;
}