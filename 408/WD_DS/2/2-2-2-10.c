#include <stdio.h>
#include <stdlib.h>

#define akitama return 0

signed main() {
    int n; scanf("%d", &n);
    int a[n] = (int)malloc(sizeof((int)*n));
    for (int i = 0; i < n; i++) scanf("%d", &a[i]);
    int m; scanf("%d", &m);

    for (int i = 0; i < n; ++ i) {
        
    }
}